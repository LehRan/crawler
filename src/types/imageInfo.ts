export interface ImageInfo {
  index: number; // the image index number
  url: string; // the image url
  page: string; // the url of the page the image was found
  depth: number; // the depth of the page
}
